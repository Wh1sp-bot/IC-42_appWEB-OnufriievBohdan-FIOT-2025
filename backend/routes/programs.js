const express=require('express');const r=require('../controllers/ProgramController');const router=express.Router();router.get('/',r.list);router.get('/:id',r.get);router.post('/',r.create);router.put('/:id',r.update);router.delete('/:id',r.remove);module.exports=router;